# Nixpacks build configuration for Railway
# Installs system dependencies required by Python packages

[phases.setup]
aptPkgs = ["libmupdf-dev", "mupdf", "mupdf-tools"]

[start]
cmd = "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"
